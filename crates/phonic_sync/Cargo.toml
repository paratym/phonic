[package]
name = "phonic_sync"
version = "0.0.1"
edition = "2021"

[features]
signal = ["dep:phonic_signal"]

[dependencies]
phonic_signal = { version = "0.0.1", path = "../phonic_signal", optional = true }
